name: Automatic Deployment

on: push


env:
  TOKEN: ${{ secrets.TOKEN }}

jobs:
  deploy:
    runs-on: ubuntu-18.04
    steps:
      - name: Git checkout
        uses: actions/checkout@v2
#        with:
#          submodules: true  # Fetch Hugo themes

      - name: Setup hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: "0.74.1"

      - name: Build and deploy
        uses: victoriadrake/hugo-latest-cd@master

#      - name: Build
#        run: hugo

#      - name: Deploy
#        uses: peaceiris/actions-gh-pages@v3
#        env:
#          github_token: ${{ secrets.TOKEN }}
          #TARGET_REPO: temuller/astro-visualization
#          publish_dir: ./docs
          #   keep_files: true
          #user_name: temuller
          #user_email: <t.e.muller-bravo@soton.ac.uk>
#          publish_branch: master
        #   cname: example.com

